"use client";

import { BarChart3, Bell, BookOpen, Bookmark, Check, ChevronDown, ChevronLeft, ChevronRight, Coffee, Heart, Home, Leaf, Menu, MoreHorizontal, Moon, PenLine, Plus, Search, Send, Share2, Sparkles, Sun, Users, X } from "lucide-react";
import type { User } from "@supabase/supabase-js";
import { useEffect, useState } from "react";
import { createClient } from "../lib/supabase/client";

type Story = { id: number; title: string; excerpt: string; author: string; handle: string; initials: string; category: string; readTime: string; date: string; accent: string; likes: number };
const stories: Story[] = [
  { id: 1, title: "The last table by the window", excerpt: "Every morning at 8:15, the cafe filled with the same quiet ritual. Until one Tuesday, a note appeared beneath my cup.", author: "Mina Park", handle: "minapark", initials: "MP", category: "Quiet moments", readTime: "4 min read", date: "Today", accent: "sage", likes: 248 },
  { id: 2, title: "A map of all the places I miss", excerpt: "I started with the corner shop on Alder Street. Then the list grew into a city made entirely of small goodbyes.", author: "Jules Rowan", handle: "julesrowan", initials: "JR", category: "Personal essays", readTime: "7 min read", date: "Yesterday", accent: "terracotta", likes: 183 },
  { id: 3, title: "The art of leaving a little early", excerpt: "There is a particular softness to being the first one home. I am learning to call it a life, not an escape.", author: "Theo Bell", handle: "theobell", initials: "TB", category: "In progress", readTime: "3 min read", date: "2 days ago", accent: "mustard", likes: 97 }
];
const PAGE_LIMIT = 620;
const navItems = [{ label: "Discover", icon: Home }, { label: "Following", icon: Users }, { label: "Dashboard", icon: BarChart3 }];
const chartData = [34, 48, 42, 67, 55, 72, 64, 84, 76, 92, 87, 100];
const supabase = createClient();

export default function HomePage() {
  const [view, setView] = useState("Discover");
  const [theme, setTheme] = useState<"light" | "dark">("light");
  const [menuOpen, setMenuOpen] = useState(false);
  const [readingStory, setReadingStory] = useState<Story | null>(null);
  const [liked, setLiked] = useState<number[]>([]);
  const [saved, setSaved] = useState<number[]>([2]);
  const [following, setFollowing] = useState<string[]>(["minapark"]);
  const [toast, setToast] = useState("");
  const [pages, setPages] = useState([""]);
  const [activePage, setActivePage] = useState(0);
  const [storyTitle, setStoryTitle] = useState("");
  const [user, setUser] = useState<User | null>(null);
  const [authOpen, setAuthOpen] = useState(false);
  const [authMode, setAuthMode] = useState<"login" | "signup">("login");
  const [authEmail, setAuthEmail] = useState("");
  const [authPassword, setAuthPassword] = useState("");
  const [authError, setAuthError] = useState("");
  const [authLoading, setAuthLoading] = useState(false);
  useEffect(() => {
    supabase.auth.getUser().then(({ data }) => setUser(data.user));
    const { data: listener } = supabase.auth.onAuthStateChange((_event, session) => setUser(session?.user ?? null));
    return () => listener.subscription.unsubscribe();
  }, [supabase]);
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(""), 2400); };
  const handleAuth = async (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault(); setAuthLoading(true); setAuthError("");
    const result = authMode === "login"
      ? await supabase.auth.signInWithPassword({ email: authEmail, password: authPassword })
      : await supabase.auth.signUp({ email: authEmail, password: authPassword });
    setAuthLoading(false);
    if (result.error) { setAuthError(result.error.message); return; }
    setAuthOpen(false); setAuthEmail(""); setAuthPassword("");
    notify(authMode === "login" ? "Welcome back" : "Check your email to confirm your account");
  };
  const handleSignOut = async () => { await supabase.auth.signOut(); notify("You have been signed out"); };
  const toggleLike = (story: Story) => setLiked((current) => current.includes(story.id) ? current.filter((id) => id !== story.id) : [...current, story.id]);
  const toggleSave = (story: Story) => { setSaved((current) => current.includes(story.id) ? current.filter((id) => id !== story.id) : [...current, story.id]); notify(saved.includes(story.id) ? "Removed from your shelf" : "Saved to your shelf"); };
  const updatePage = (value: string) => { const next = [...pages]; next[activePage] = value; setPages(next); };
  const addPage = () => { setPages([...pages, ""]); setActivePage(pages.length); };
  const toggleFollow = (story: Story) => { setFollowing((current) => current.includes(story.handle) ? current.filter((handle) => handle !== story.handle) : [...current, story.handle]); notify(following.includes(story.handle) ? `Unfollowed ${story.author}` : `Following ${story.author}`); };

  return <div className={`app-shell ${theme === "dark" ? "dark-mode" : ""}`}>
    <aside className={`sidebar ${menuOpen ? "sidebar-open" : ""}`}>
      <div className="brand-lockup"><div className="brand-mark"><Coffee size={19} /></div><span>Flip Stories</span></div>
      <button className="profile-mini" onClick={() => user ? handleSignOut() : setAuthOpen(true)}><div className="avatar avatar-plum">{user ? (user.email?.[0] ?? "U").toUpperCase() : "NS"}</div><div><strong>{user ? user.email : "Sign in to Flip Stories"}</strong><span>{user ? "Sign out" : "Read, write, and follow"}</span></div><ChevronDown size={15} /></button>
      <nav className="main-nav" aria-label="Main navigation">{navItems.map(({ label, icon: Icon }) => <button className={`nav-item ${view === label ? "active" : ""}`} key={label} onClick={() => { setView(label); setMenuOpen(false); }}><Icon size={18} /><span>{label}</span>{label === "Following" && <span className="nav-count">4</span>}</button>)}</nav>
      <div className="nav-section-label">Your space</div><nav className="main-nav"><button className={`nav-item ${view === "Write" ? "active" : ""}`} onClick={() => { setView("Write"); setMenuOpen(false); }}><PenLine size={18} /><span>Write a story</span></button><button className="nav-item" onClick={() => notify("Your shelf is coming with you")}><Bookmark size={18} /><span>My shelf</span><span className="nav-count">2</span></button></nav>
      <div className="sidebar-bottom"><div className="sidebar-note"><Sparkles size={17} /><p><strong>Small stories matter.</strong><br />Make room for yours.</p></div><button className="theme-toggle" onClick={() => setTheme(theme === "light" ? "dark" : "light")}>{theme === "light" ? <Moon size={16} /> : <Sun size={16} />}<span>{theme === "light" ? "Night mode" : "Day mode"}</span><span className="toggle-track"><span /></span></button></div>
    </aside>
    <main className="main-content"><header className="topbar"><button className="mobile-menu" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle menu"><Menu size={21} /></button><div className="crumb"><span>Library</span><ChevronRight size={14} /><strong>{view}</strong></div><div className="top-actions"><label className="search-box"><Search size={17} /><input placeholder="Search stories" aria-label="Search stories" /><kbd>⌘ K</kbd></label><button className="icon-button notification-button" onClick={() => notify("You are all caught up")} aria-label="Notifications"><Bell size={18} /><span /></button><div className="avatar avatar-plum avatar-small">NS</div></div></header>
      {view === "Discover" && <Discover setView={setView} setReadingStory={setReadingStory} stories={stories} liked={liked} saved={saved} following={following} onLike={toggleLike} onSave={toggleSave} onFollow={toggleFollow} />}
      {view === "Following" && <FollowingView setView={setView} stories={stories.filter((story) => following.includes(story.handle))} liked={liked} saved={saved} following={following} onLike={toggleLike} onSave={toggleSave} onFollow={toggleFollow} onRead={setReadingStory} />}
      {view === "Dashboard" && <Dashboard onWrite={() => setView("Write")} />}
      {view === "Write" && <Writer setView={setView} storyTitle={storyTitle} setStoryTitle={setStoryTitle} pages={pages} activePage={activePage} setActivePage={setActivePage} updatePage={updatePage} addPage={addPage} notify={notify} />}
    </main>
    {readingStory && <ReaderModal story={readingStory} liked={liked.includes(readingStory.id)} saved={saved.includes(readingStory.id)} onClose={() => setReadingStory(null)} onLike={() => toggleLike(readingStory)} onSave={() => toggleSave(readingStory)} onShare={() => notify("Story link copied to your clipboard")} />}
    {authOpen && <AuthModal mode={authMode} setMode={setAuthMode} email={authEmail} setEmail={setAuthEmail} password={authPassword} setPassword={setAuthPassword} error={authError} loading={authLoading} onSubmit={handleAuth} onClose={() => setAuthOpen(false)} />}
    {toast && <div className="toast"><Check size={16} /> {toast}</div>}
  </div>;
}

function Discover({ setView, setReadingStory, stories, liked, saved, following, onLike, onSave, onFollow }: { setView: (view: string) => void; setReadingStory: (story: Story) => void; stories: Story[]; liked: number[]; saved: number[]; following: string[]; onLike: (story: Story) => void; onSave: (story: Story) => void; onFollow: (story: Story) => void }) {
  return <section className="content-wrap"><div className="welcome-row"><div><p className="eyebrow"><Leaf size={14} /> Tuesday, September 21</p><h1>A quiet place for <em>good stories.</em></h1><p className="subtitle">Pull up a chair. There&apos;s something new to read.</p></div><button className="primary-button" onClick={() => setView("Write")}><PenLine size={17} /> Write a story</button></div><div className="feature-strip"><div className="feature-copy"><span className="section-kicker">Editor&apos;s pick <span>✦</span></span><h2>Somewhere between<br /><em>then and now</em></h2><p>A tender reflection on the places that shape us, by Mina Park.</p><button className="text-button" onClick={() => setReadingStory(stories[0])}>Read the story <ChevronRight size={15} /></button></div><div className="feature-art" aria-hidden="true"><div className="sun-disc" /><div className="art-window" /><div className="art-table" /><div className="art-cup"><Coffee size={28} /></div></div></div><div className="section-heading"><div><span className="section-kicker">The daily pour</span><h2>Fresh from the community</h2></div><button className="text-button muted-button" onClick={() => setView("Following")}>View all <ChevronRight size={15} /></button></div><div className="story-list">{stories.slice(1).map((story) => <StoryCard key={story.id} story={story} liked={liked.includes(story.id)} saved={saved.includes(story.id)} following={following.includes(story.handle)} onLike={() => onLike(story)} onSave={() => onSave(story)} onFollow={() => onFollow(story)} onRead={() => setReadingStory(story)} />)}</div></section>;
}

function FollowingView({ setView, stories, liked, saved, following, onLike, onSave, onFollow, onRead }: { setView: (view: string) => void; stories: Story[]; liked: number[]; saved: number[]; following: string[]; onLike: (story: Story) => void; onSave: (story: Story) => void; onFollow: (story: Story) => void; onRead: (story: Story) => void }) {
  return <section className="content-wrap compact-wrap"><div className="page-heading"><div><p className="eyebrow"><Users size={14} /> Your circle</p><h1>Stories from people <em>you follow.</em></h1><p className="subtitle">A little closer to home.</p></div><button className="primary-button" onClick={() => setView("Write")}><PenLine size={17} /> Write a story</button></div><div className="following-banner"><div className="stacked-avatars"><div className="avatar avatar-sage">MP</div><div className="avatar avatar-rose">JR</div><div className="avatar avatar-ochre">TB</div></div><span>You&apos;re following <strong>{following.length} writers</strong></span><button className="text-button" onClick={() => setView("Discover")}>Find more writers <ChevronRight size={15} /></button></div><div className="story-list following-list">{stories.map((story) => <StoryCard key={story.id} story={story} liked={liked.includes(story.id)} saved={saved.includes(story.id)} following={following.includes(story.handle)} onLike={() => onLike(story)} onSave={() => onSave(story)} onFollow={() => onFollow(story)} onRead={() => onRead(story)} />)}</div></section>;
}

function StoryCard({ story, liked, saved, following, onLike, onSave, onFollow, onRead }: { story: Story; liked: boolean; saved: boolean; following: boolean; onLike: () => void; onSave: () => void; onFollow: () => void; onRead: () => void }) {
  return <article className="story-card"><div className={`story-art story-art-${story.accent}`}><div className="art-shape-one" /><div className="art-shape-two" /><BookOpen size={22} /></div><div className="story-card-main"><div className="story-card-meta"><span className="story-category">{story.category}</span><span>{story.date}</span></div><h3>{story.title}</h3><p>{story.excerpt}</p><div className="story-card-bottom"><button className="author-chip" onClick={onFollow}><span className={`avatar avatar-${story.accent}`}>{story.initials}</span><span><strong>{story.author}</strong><small>{following ? "Following" : `@${story.handle}`}</small></span></button><span className="read-time">{story.readTime}</span><div className="story-actions"><button className={liked ? "liked" : ""} onClick={onLike} aria-label="Like story"><Heart size={16} fill={liked ? "currentColor" : "none"} /><span>{story.likes + (liked ? 1 : 0)}</span></button><button className={saved ? "saved" : ""} onClick={onSave} aria-label="Save story"><Bookmark size={16} fill={saved ? "currentColor" : "none"} /></button><button onClick={onRead} className="read-link">Read <ChevronRight size={14} /></button></div></div></div></article>;
}

function Dashboard({ onWrite }: { onWrite: () => void }) {
  return <section className="content-wrap dashboard-wrap"><div className="page-heading"><div><p className="eyebrow"><BarChart3 size={14} /> Your overview</p><h1>Good morning, <em>Nora.</em></h1><p className="subtitle">Here&apos;s how your stories are finding their people.</p></div><button className="primary-button" onClick={onWrite}><PenLine size={17} /> New story</button></div><div className="stat-grid"><StatCard label="Total views" value="12,840" change="+18.4%" icon={<BookOpen size={17} />} /><StatCard label="Likes" value="1,286" change="+12.8%" icon={<Heart size={17} />} /><StatCard label="Favorites" value="438" change="+9.2%" icon={<Bookmark size={17} />} /><StatCard label="Shares" value="196" change="+24.6%" icon={<Share2 size={17} />} /></div><div className="analytics-grid"><div className="analytics-card chart-card"><div className="analytics-header"><div><span className="section-kicker">Audience pulse</span><h2>People are reading</h2></div><select defaultValue="Last 30 days"><option>Last 30 days</option><option>Last 7 days</option></select></div><div className="chart-total"><strong>12,840</strong><span><span className="up-dot">↗</span> 18.4% <small>vs. last month</small></span></div><div className="bar-chart">{chartData.map((height, index) => <div className="bar-column" key={index}><div className="bar" style={{ height: `${height}%` }} /><span>{["Aug 22", "", "", "Aug 29", "", "", "Sep 5", "", "", "Sep 12", "", "Sep 19"][index]}</span></div>)}</div></div><div className="analytics-card audience-card"><div className="analytics-header"><div><span className="section-kicker">Your stories</span><h2>Top performers</h2></div><button className="icon-button"><MoreHorizontal size={18} /></button></div><div className="performance-list"><div><span className="rank">01</span><span className="mini-art mini-sage"><BookOpen size={14} /></span><span><strong>The last table by the window</strong><small>8,420 views</small></span><span className="performance-change">+32%</span></div><div><span className="rank">02</span><span className="mini-art mini-terracotta"><BookOpen size={14} /></span><span><strong>Things I learned from rain</strong><small>2,918 views</small></span><span className="performance-change">+18%</span></div><div><span className="rank">03</span><span className="mini-art mini-mustard"><BookOpen size={14} /></span><span><strong>A note to my younger self</strong><small>1,502 views</small></span><span className="performance-change">+9%</span></div></div><button className="text-button full-button">View all stories <ChevronRight size={15} /></button></div></div><div className="dashboard-bottom"><div className="recent-heading"><h2>Recent activity</h2><button className="text-button">See all <ChevronRight size={15} /></button></div><div className="activity-row"><div className="avatar avatar-rose">JR</div><p><strong>Jules Rowan</strong> saved <em>The last table by the window</em></p><span>2h ago</span></div><div className="activity-row"><div className="avatar avatar-ochre">TB</div><p><strong>Theo Bell</strong> started following you</p><span>5h ago</span></div></div></section>;
}

function StatCard({ label, value, change, icon }: { label: string; value: string; change: string; icon: React.ReactNode }) { return <div className="stat-card"><div className="stat-icon">{icon}</div><span>{label}</span><strong>{value}</strong><small><span>↗</span> {change} <em>this month</em></small></div>; }

function Writer({ setView, storyTitle, setStoryTitle, pages, activePage, setActivePage, updatePage, addPage, notify }: { setView: (view: string) => void; storyTitle: string; setStoryTitle: (value: string) => void; pages: string[]; activePage: number; setActivePage: (page: number) => void; updatePage: (value: string) => void; addPage: () => void; notify: (message: string) => void }) {
  return <section className="content-wrap writer-wrap"><div className="writer-heading"><div><p className="eyebrow"><PenLine size={14} /> Your writing desk</p><h1>Make a little <em>room.</em></h1><p className="subtitle">Stories don&apos;t have to be loud to stay with someone.</p></div><button className="quiet-button" onClick={() => setView("Discover")}><X size={16} /> Close</button></div><div className="writer-layout"><div className="paper-card"><div className="paper-topline"><span>New story</span><span>Saved just now <Check size={14} /></span></div><input className="title-input" value={storyTitle} onChange={(event) => setStoryTitle(event.target.value)} placeholder="Give your story a title..." /><div className="writing-tools"><button aria-label="Bold"><span className="tool-bold">B</span></button><button aria-label="Italic"><span className="tool-italic">I</span></button><span /><button aria-label="Add a photo"><Plus size={17} /></button><button aria-label="More writing tools"><MoreHorizontal size={18} /></button></div><textarea className="story-textarea" value={pages[activePage]} onChange={(event) => updatePage(event.target.value)} placeholder="Start with the moment you knew..." /><div className={`page-limit ${pages[activePage].length >= PAGE_LIMIT ? "page-limit-full" : ""}`}><span>{pages[activePage].length} / {PAGE_LIMIT}</span>{pages[activePage].length >= PAGE_LIMIT && <span>Page full</span>}</div>{pages[activePage].length >= PAGE_LIMIT && <button className="new-page-prompt" onClick={addPage}><Plus size={16} /> This page is full. Start a new page</button>}<div className="paper-footer"><span>Page {activePage + 1} of {pages.length}</span><div className="page-controls"><button onClick={() => setActivePage(Math.max(0, activePage - 1))} disabled={activePage === 0}><ChevronLeft size={16} /></button><button onClick={() => setActivePage(Math.min(pages.length - 1, activePage + 1))} disabled={activePage === pages.length - 1}><ChevronRight size={16} /></button></div></div></div><aside className="writer-side-panel"><div className="side-panel-header"><span>Story details</span><Sparkles size={16} /></div><label>Topic or feeling<input placeholder="e.g. growing up, home" /></label><label>Who can read this?<select defaultValue="Everyone"><option>Everyone</option><option>Followers only</option><option>Only me</option></select></label><div className="story-tip"><Coffee size={17} /><p><strong>A tiny prompt</strong><br />Write about a sound you haven&apos;t heard in years.</p><button onClick={() => notify("Prompt saved to your desk")}>Save prompt</button></div><button className="publish-button" onClick={() => notify("Your story is ready to publish")}>Publish story <Send size={16} /></button><button className="draft-button" onClick={() => notify("Draft saved")}>Save as draft</button></aside></div></section>;
}

function ReaderModal({ story, liked, saved, onClose, onLike, onSave, onShare }: { story: Story; liked: boolean; saved: boolean; onClose: () => void; onLike: () => void; onSave: () => void; onShare: () => void }) { return <div className="modal-backdrop" onClick={onClose}><article className="reader-modal" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close story"><X size={19} /></button><div className={`reader-art reader-art-${story.accent}`}><div className="reader-art-circle" /><Coffee size={37} /></div><div className="reader-content"><span className="story-category">{story.category}</span><h2>{story.title}</h2><div className="reader-author"><span className={`avatar avatar-${story.accent}`}>{story.initials}</span><span><strong>{story.author}</strong><small>{story.readTime} · {story.date}</small></span><button onClick={onSave} className="reader-save">{saved ? "Saved" : "Save story"} <Bookmark size={15} fill={saved ? "currentColor" : "none"} /></button></div><div className="reader-body"><p>{story.excerpt}</p><p>There are days when a story begins before we know to call it one. It lives in the small details: the warmth of a cup held between both hands, the familiar face across the room, the pause before a door opens.</p><p>Maybe that is why we keep coming back to the same places. Not because they never change, but because they let us notice when we do.</p></div><div className="reader-actions"><button onClick={onLike} className={liked ? "liked" : ""}><Heart size={17} fill={liked ? "currentColor" : "none"} /> {liked ? "Liked" : "Like story"}</button><button onClick={onShare}><Share2 size={17} /> Share</button></div></div></article></div>; }

function AuthModal({ mode, setMode, email, setEmail, password, setPassword, error, loading, onSubmit, onClose }: { mode: "login" | "signup"; setMode: (mode: "login" | "signup") => void; email: string; setEmail: (value: string) => void; password: string; setPassword: (value: string) => void; error: string; loading: boolean; onSubmit: (event: React.FormEvent<HTMLFormElement>) => void; onClose: () => void }) {
  return <div className="modal-backdrop" onClick={onClose}><section className="auth-modal" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close account form"><X size={19} /></button><div className="auth-mark"><Coffee size={22} /></div><p className="eyebrow">Flip Stories</p><h2>{mode === "login" ? "Welcome back." : "Make room for your story."}</h2><p className="auth-intro">{mode === "login" ? "Your little corner of the reading room is waiting." : "Create an account to write, save, and follow."}</p><form onSubmit={onSubmit} className="auth-form"><label>Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required placeholder="you@example.com" /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} required minLength={6} placeholder="At least 6 characters" /></label>{error && <p className="auth-error">{error}</p>}<button className="publish-button auth-submit" disabled={loading}>{loading ? "Please wait..." : mode === "login" ? "Log in" : "Create account"}</button></form><p className="auth-switch">{mode === "login" ? "New to Flip Stories?" : "Already have an account?"} <button onClick={() => { setMode(mode === "login" ? "signup" : "login"); }}> {mode === "login" ? "Create an account" : "Log in"}</button></p></section></div>;
}